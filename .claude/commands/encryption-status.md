Give me the step-by-step process to check encryption status across the environment. Cover: BitLocker status on Windows devices (Intune > Devices > [device] > Hardware or Endpoint security > Disk encryption), email encryption (OME - Office Message Encryption) configuration, SharePoint/OneDrive encryption (always on in M365), check if sensitivity labels enforce encryption, check TLS for email in transit (transport rules), Azure Information Protection status. Use placeholders. Portal steps first. Never ask me for details. Just give the full procedure.
