IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to verify DKIM, DMARC, and SPF records. Cover: check SPF record (nslookup -type=txt [DOMAIN] or MXToolbox), verify it includes Microsoft's servers (include:spf.protection.outlook.com), check DKIM (security.microsoft.com > Email & collaboration > Policies > DKIM), enable DKIM signing for the domain, check DMARC record (nslookup -type=txt _dmarc.[DOMAIN]), recommended DMARC policy progression (none > quarantine > reject), how to read DMARC aggregate reports. Explain each protocol in plain English. Use placeholder [DOMAIN]. Never ask me for details. Just give the full procedure.
