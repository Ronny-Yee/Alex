Give me the step-by-step process to bulk-rename devices to match our naming convention (DT-FirstName,LastName for desktops, LT-FirstName,LastName for laptops). Cover: export all devices from Intune, identify non-compliant names, rename via Intune portal (Devices > All devices > [device] > Rename), rename via PowerShell (Invoke-MgGraphRequest or Rename-Computer remotely), verify rename after device syncs. Use placeholders. Warn that rename requires device restart. Portal steps first, PowerShell bulk script second. Never ask me for details. Just give the full procedure.
