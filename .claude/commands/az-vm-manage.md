IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to manage Azure VMs. Cover: create a VM (portal.azure.com > Virtual machines > + Create), select image, size, region, networking (VNet, subnet, NSG, public IP), authentication (SSH key vs password), start/stop/restart a VM, resize a VM, create a snapshot, set up auto-shutdown to save costs, connect via RDP (Windows) or SSH (Linux), check VM metrics (CPU, memory, disk, network). Use placeholders. Portal steps first, Azure CLI second. Never ask me for details. Just give the full procedure.
