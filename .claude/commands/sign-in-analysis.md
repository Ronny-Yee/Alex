Give me the step-by-step process to analyze sign-in patterns for a user. Cover: navigate to sign-in logs (entra.microsoft.com > Identity > Users > [user] > Sign-in logs), filter by date, status (success/failure), application, location, understand sign-in details (IP, location, device, browser, CA policy applied, MFA method), identify suspicious patterns (impossible travel, unfamiliar locations, brute-force attempts, legacy auth usage), correlate with risky sign-ins report. Use placeholder [UPN]. Portal steps first. Never ask me for details. Just give the full procedure.
