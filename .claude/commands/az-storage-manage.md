Give me the step-by-step process to manage Azure Storage. Cover: create a storage account (portal.azure.com > Storage accounts > + Create), storage types (Blob, File, Queue, Table), create a blob container, upload/download files, set access tiers (Hot, Cool, Cold, Archive), configure access policies (public, private, SAS tokens), set lifecycle management rules (auto-tier or delete old blobs), configure CORS if needed, check storage metrics. Use placeholders. Portal steps first, Azure CLI second. Never ask me for details. Just give the full procedure.
