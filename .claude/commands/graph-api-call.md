Give me the step-by-step process to make Microsoft Graph API calls. Cover: what Graph API is (single endpoint for all M365 data), authentication (Connect-MgGraph with scopes, or app registration with client secret), common endpoints (users, groups, devices, mail, calendar, files), GET vs POST vs PATCH vs DELETE, pagination (handling @odata.nextLink), filtering and selecting fields ($filter, $select, $top, $orderby), batch requests, Graph Explorer tool (developer.microsoft.com/graph/graph-explorer) for testing. Explain in plain English with working examples. Use placeholders. Never ask me for details. Just give the full guide.
