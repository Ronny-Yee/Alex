Give me the step-by-step process to export a full device inventory. Cover: export from Intune (intune.microsoft.com > Devices > All devices > Export), what columns are included (device name, OS, compliance, last check-in, primary user, serial, model, storage, encryption), how to export to CSV, how to use Graph API for custom queries (filter by OS, compliance state, last check-in older than X days), how to build a recurring inventory report. Use placeholders. Portal steps first, PowerShell/Graph second. Never ask me for details. Just give the full procedure.
