IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me a comprehensive Linux server troubleshooting guide. Cover: check system load (top, htop, uptime), check memory usage (free -h), check disk usage (df -h, du -sh), check running processes (ps aux, systemctl status), check network connections (ss -tulpn, ip a), check DNS (cat /etc/resolv.conf, dig), check firewall (ufw status, firewalld, iptables -L), check logs (journalctl -u [SERVICE], tail -f /var/log/syslog), check cron jobs (crontab -l), restart a service (systemctl restart [SERVICE]). Explain every command in plain English. Use placeholders. Never ask me for details. Just give the full guide.
