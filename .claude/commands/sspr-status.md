IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to check and configure Self-Service Password Reset (SSPR). Cover: check if SSPR is enabled (entra.microsoft.com > Protection > Password reset), which groups are enabled, what authentication methods are required (phone, email, security questions), how SSPR works with on-prem AD writeback via Entra Connect, how to test SSPR as a user, how to monitor SSPR usage in audit logs. Explain in plain English. Use placeholders. Never ask me for details. Just give the full procedure.
