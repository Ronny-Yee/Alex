IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me a comprehensive Python debugging guide. Cover: read tracebacks (bottom-up, identify file, line, error type), common errors (ImportError, AttributeError, TypeError, KeyError, IndexError, FileNotFoundError) and fixes for each, use print debugging, use Python debugger (pdb/breakpoint()), use logging module, profile slow code (cProfile, timeit), common package issues (pip install, virtual environments, version conflicts), linting with pylint/flake8. Explain everything in plain English for someone learning Python. Use placeholders. Never ask me for details. Just give the full guide.
