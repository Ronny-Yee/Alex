IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to set up Meraki Client VPN for a remote user. Cover: server-side configuration (dashboard.meraki.com > Security & SD-WAN > Client VPN), authentication method (Meraki cloud, RADIUS, AD), enable Client VPN, configure subnet and DNS, client-side setup on Windows (Settings > Network > VPN > Add VPN connection), client-side setup on Mac, troubleshoot common issues (connection timeout, auth failure, split tunnel vs full tunnel), verify user can access internal resources over VPN. Use placeholders. Never ask me for details. Just give the full procedure.
