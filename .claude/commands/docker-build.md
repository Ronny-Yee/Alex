IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to build and manage Docker containers. Cover: write a Dockerfile (FROM, RUN, COPY, EXPOSE, CMD explained), multi-stage builds for smaller images, build image (docker build -t [IMAGE_NAME] .), run container (docker run), view running containers (docker ps), view logs (docker logs), exec into container (docker exec -it), push to registry (docker push), docker-compose for multi-container apps, common Dockerfile best practices (layer caching, .dockerignore, non-root user). Explain every command in plain English. Use placeholders. Never ask me for details. Just give the full procedure.
