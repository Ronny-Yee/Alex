Give me a step-by-step guide to troubleshoot API issues. Cover: check HTTP status codes (200, 301, 400, 401, 403, 404, 429, 500, 502, 503 — what each means), check request/response headers, check request body and content type, test with curl or Postman, check authentication flow (API key, Bearer token, OAuth2), check rate limiting (429 Too Many Requests), check CORS issues (browser console errors), check SSL/TLS issues, check DNS resolution, common Graph API errors for M365. Explain every status code and fix in plain English. Use placeholders. Never ask me for details. Just give the full guide.
