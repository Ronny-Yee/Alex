Give me the step-by-step process to audit all admin role assignments in Entra. Cover: list all users with admin roles (entra.microsoft.com > Identity > Roles & admins > All roles), identify over-privileged accounts (Global Admin when User Admin would do), check for permanent vs eligible assignments (PIM), verify MFA is enforced on all admin accounts, check that admin accounts don't have mailboxes or regular user activity, verify break-glass accounts. Use placeholders. Portal steps first. Never ask me for details. Just give the full procedure.
