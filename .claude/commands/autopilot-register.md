Give me the step-by-step process to register a new device in Autopilot. Cover: capture hardware hash (Get-WindowsAutopilotInfo script from PowerShell Gallery), import CSV into Autopilot (intune.microsoft.com > Devices > Enrollment > Windows Autopilot > Devices > Import), assign group tag, verify dynamic group membership picks it up, assign deployment profile, test by resetting the device. Use placeholder [DEVICE_SERIAL], [GROUP_TAG]. Portal steps first, PowerShell for hash capture. Never ask me for details. Just give the full procedure.
