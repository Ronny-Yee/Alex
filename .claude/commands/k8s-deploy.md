IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to create and manage Kubernetes deployments. Cover: write a deployment YAML (apiVersion, kind, metadata, spec, replicas, containers, image, ports, resources, readiness/liveness probes), apply deployment (kubectl apply -f), check rollout status (kubectl rollout status), rolling update strategy, rollback a deployment (kubectl rollout undo), scale replicas (kubectl scale), expose via Service (ClusterIP, NodePort, LoadBalancer), check endpoints. Explain every field in the YAML. Use placeholders. Never ask me for details. Just give the full procedure.
