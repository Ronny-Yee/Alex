IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me a reusable template for building PowerShell reports with CSV and HTML export. Cover: collect data with Microsoft Graph or Exchange cmdlets, format into custom objects (PSCustomObject), export to CSV (Export-Csv), build HTML report with CSS styling (ConvertTo-Html with custom CSS header), export to HTML, email report automatically (Send-MailMessage or Graph API), common reports (license usage, mailbox sizes, group memberships, sign-in activity, device compliance). Include a complete working template. Explain every line in plain English. Never ask me for details. Just give the full template.
