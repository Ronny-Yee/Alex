Give me the step-by-step process to set up an Intermedia Unite call queue. Cover: navigate to call queues in admin portal (Phone system > Call queues), create queue, add agents (extensions), set ring pattern (simultaneous, round-robin, longest idle), set max wait time, configure overflow routing (voicemail, another queue, external number), set after-hours routing, configure hold music, set max queue size, test with multiple calls. Use placeholders. Never ask me for details. Just give the full procedure.
