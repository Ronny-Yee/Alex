Give me the step-by-step process to add, remove, or list email aliases. Cover: manage aliases via admin center (admin.microsoft.com > Users > [user] > Mail tab > Manage email aliases), add alias, set primary email (reply-from address), remove old alias, aliases on shared mailboxes (admin.exchange.microsoft.com > Recipients > Mailboxes > [mailbox] > Email addresses), proxy address format (SMTP: primary, smtp: alias), Entra Connect sync impact on aliases for hybrid users. Use placeholders. Portal steps first. Never ask me for details. Just give the full procedure.
