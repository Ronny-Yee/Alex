IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to troubleshoot common Terraform issues. Cover: state lock errors (terraform force-unlock [LOCK_ID]), state drift (terraform refresh, terraform import), provider authentication errors, dependency cycles (use depends_on), "resource already exists" errors (terraform import), plan shows unexpected destroy (check for name changes, lifecycle blocks), module version conflicts, backend configuration errors. Explain every error and fix in plain English. Use placeholders. Never ask me for details. Just give the full procedure.
