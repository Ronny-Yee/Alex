IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to run and analyze a Terraform plan. Cover: terraform init (download providers), terraform plan (preview changes), read the plan output (+ create, - destroy, ~ update, -/+ replace), terraform apply (execute), terraform state list (see managed resources), terraform state show [resource], common plan issues (drift detection, resource changes outside Terraform), .tfvars for environment-specific values, backend configuration for remote state. Explain every command in plain English. Use placeholders. Never ask me for details. Just give the full procedure.
