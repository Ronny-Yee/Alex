Give me the step-by-step process to troubleshoot Docker container issues. Cover: container won't start (check logs: docker logs [CONTAINER]), container exits immediately (check CMD/ENTRYPOINT, check exit code), networking issues (docker network inspect, port mapping, DNS resolution inside container), volume mount issues (permissions, path), out of disk space (docker system df, docker system prune), image pull failures (auth, registry connectivity), high CPU/memory (docker stats). Explain every command in plain English. Use placeholders. Never ask me for details. Just give the full procedure.
