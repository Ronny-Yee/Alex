Give me the step-by-step process to create a shared mailbox and assign permissions. Cover: create shared mailbox (admin.microsoft.com > Teams & groups > Shared mailboxes > + Add), set display name and email address, assign Full Access permission (can read/send from mailbox), assign Send As permission (emails appear to come from the shared mailbox), assign Send on Behalf permission, add to Outlook (auto-mapped or manual), set mailbox auto-reply if needed, verify no license is needed for shared mailboxes under 50GB. Use placeholders. Portal steps first. Never ask me for details. Just give the full procedure.
