Give me the step-by-step process to create Windows scheduled tasks for PowerShell scripts. Cover: create via Task Scheduler GUI (taskschd.msc), set trigger (daily, weekly, on event), set action (powershell.exe -ExecutionPolicy Bypass -File [SCRIPT_PATH]), set "Run whether user is logged on or not", set "Run with highest privileges", configure for service account, common issues (script works manually but not scheduled — usually path or permissions), logging output to file, PowerShell alternative (Register-ScheduledTask cmdlet). Explain in plain English. Use placeholders. Never ask me for details. Just give the full procedure.
