IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me a step-by-step guide to analyze and fix slow SQL queries. Cover: identify slow queries (SQL Server: Activity Monitor, DMVs sys.dm_exec_query_stats; PostgreSQL: pg_stat_statements), read execution plans (EXPLAIN in PostgreSQL, SET STATISTICS IO ON in SQL Server), identify table scans vs index seeks, missing index recommendations, common fixes (add index, rewrite query, update statistics, parameterize queries), check wait statistics, check tempdb usage, check memory grants. Explain every concept in plain English. Use placeholders. Never ask me for details. Just give the full guide.
