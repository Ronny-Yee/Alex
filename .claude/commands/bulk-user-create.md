IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to bulk-create users from a CSV file. Cover: CSV template format (FirstName, LastName, UPN, Department, Title, Manager, License), import into on-prem AD using PowerShell (New-ADUser loop), force Entra Connect sync, verify users appear in M365, assign licenses in bulk, add to groups. Use placeholders like [UPN], [DEPARTMENT], [OU_PATH]. Portal steps for verification first, PowerShell for the actual bulk import. Include CSV template I can copy. Warn before any destructive operations. Never ask me for details. Just give the full procedure.
