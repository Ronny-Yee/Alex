Give me the step-by-step process to deploy an app through Intune. Cover: Win32 app (.intunewin packaging with IntuneWinAppUtil), LOB app (MSI), Microsoft Store app, web link app. For each type: how to package, upload (intune.microsoft.com > Apps > All apps > + Add), set install/uninstall commands, detection rules (file, registry, MSI code), requirements, assignment (required vs available vs uninstall), monitoring install status. Use placeholders. Portal steps first. Never ask me for details. Just give the full procedure.
