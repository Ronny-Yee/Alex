Give me the step-by-step process to check BayAlarm system status across all Family Bridges sites. Cover: which sites are on landline-based monitoring (legacy) vs internet-based (upgraded), how to verify alarm panel connectivity (check internet at site via Meraki dashboard), what to do if alarm goes offline (check network first, then contact BayAlarm support), coordination with VoIP migration (once Intermedia Unite is live, the Comcast landline feeding the alarm can be cut), BayAlarm support contact procedure. Use placeholders for site names and contact info. Never ask me for details. Just give the full procedure.
