IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to verify emergency access (break-glass) accounts. Cover: confirm at least 2 break-glass accounts exist, verify they are excluded from ALL Conditional Access policies, verify they have Global Admin role, verify they use different MFA methods than regular admins (e.g., FIDO2 key), verify they are not synced from on-prem AD (cloud-only), verify passwords are stored securely offline, check sign-in logs to confirm they are not used for daily work, set up alerts for when break-glass accounts sign in. Use placeholders. Never ask me for details. Just give the full procedure.
