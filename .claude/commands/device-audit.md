IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to do a full audit of a single device. Cover: find device in Intune (intune.microsoft.com > Devices > All devices > [DEVICE_NAME]), check compliance state, last check-in time, OS version, encryption status (BitLocker), installed apps, configuration profile status, detected apps, hardware details (serial, model, RAM, storage), primary user, Entra registration status. Flag if device name doesn't match DT-First,Last or LT-First,Last convention. Use placeholder [DEVICE_NAME]. Portal steps first. Never ask me for details. Just give the full procedure.
