IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to set up webhooks for common IT integrations. Cover: Teams incoming webhook (post alerts to a channel), Jira webhook (trigger on ticket events), Power Automate HTTP trigger, how webhooks work (URL receives JSON POST when event fires), common use cases (new ticket notification, service health alert, build completion), how to test webhooks (curl or Postman POST to the URL), securing webhooks (shared secret, IP allowlisting). Explain in plain English. Use placeholders. Never ask me for details. Just give the full procedure.
