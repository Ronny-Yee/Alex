Give me the step-by-step process to audit Azure networking. Cover: list all VNets and subnets (portal.azure.com > Virtual networks), check NSG rules (inbound and outbound), check for overly permissive rules (allow all from internet), review VNet peering configurations, check route tables, verify DNS settings, check public IP assignments, review Application Gateway or Load Balancer config if present, Network Watcher tools (connection troubleshoot, IP flow verify, NSG flow logs). Use placeholders. Portal steps first. Never ask me for details. Just give the full procedure.
