IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me a complete guide to Git workflows for IT projects. Cover: branching strategy (main, develop, feature branches, hotfix branches), create a branch (git checkout -b [BRANCH_NAME]), commit best practices (small commits, clear messages), push and create PR (git push -u origin [BRANCH_NAME]), PR review checklist, merge strategies (merge commit, squash, rebase), resolve merge conflicts step by step, git stash for temporary work, git log and git blame for investigating history, .gitignore best practices. Explain every command in plain English. Use placeholders. Never ask me for details. Just give the full guide.
