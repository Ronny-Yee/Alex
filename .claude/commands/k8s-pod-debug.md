IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to debug Kubernetes pod failures. Cover: check pod status (kubectl get pods), describe pod for events (kubectl describe pod [POD_NAME]), check pod logs (kubectl logs [POD_NAME]), check previous container logs (kubectl logs [POD_NAME] --previous), common pod states (Pending, CrashLoopBackOff, ImagePullBackOff, OOMKilled, Evicted) and what causes each, check resource limits/requests, exec into pod (kubectl exec -it [POD_NAME] -- /bin/sh), check node resources (kubectl top nodes). Explain every command in plain English. Use placeholders. Never ask me for details. Just give the full procedure.
