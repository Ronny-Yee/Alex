Give me a step-by-step guide to diagnose and fix connection pool issues. Cover: what connection pooling is (in plain English), symptoms of pool exhaustion (timeouts, "too many connections" errors, slow application response), check current connections (SQL Server: sys.dm_exec_connections; PostgreSQL: pg_stat_activity), check pool configuration (min/max size, timeout, idle timeout), common causes (connection leaks, long-running queries, missing connection.close()), optimal pool sizing formula, monitoring pool health. Explain in plain English. Use placeholders. Never ask me for details. Just give the full guide.
