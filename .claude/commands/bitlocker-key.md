Give me the step-by-step process to retrieve a BitLocker recovery key. Cover: find device in Intune (intune.microsoft.com > Devices > All devices > [DEVICE_NAME] > Recovery keys), find in Entra (entra.microsoft.com > Identity > Devices > All devices > [device] > BitLocker keys), understand key ID vs recovery key, when a user needs the key (TPM change, boot change, BIOS update), how to rotate the key remotely from Intune. Use placeholder [DEVICE_NAME]. Portal steps first. Never ask me for details. Just give the full procedure.
