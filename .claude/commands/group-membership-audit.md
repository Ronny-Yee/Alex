IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to audit group memberships. Cover: list all members of a specific group (security, distribution, M365 group), show nested group expansion, find users who are in no groups, find groups with no members (empty groups), compare two groups for overlap, export group membership to CSV. Use placeholder [GROUP_NAME]. Entra portal steps first (Identity > Groups > All Groups > [group] > Members), PowerShell with Get-MgGroupMember second. Never ask me for details. Just give the full procedure.
