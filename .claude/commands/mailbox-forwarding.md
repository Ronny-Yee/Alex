Give me the step-by-step process to check and manage mailbox forwarding. Cover: check SMTP forwarding (admin.exchange.microsoft.com > Recipients > Mailboxes > [mailbox] > Mail flow settings > Email forwarding), check inbox rules that auto-forward (PowerShell: Get-InboxRule), difference between SMTP forwarding and inbox rules, how to disable all forwarding, how to set up approved forwarding, how to block external forwarding org-wide via transport rule. Use placeholder [MAILBOX]. Portal steps first. Warn about unauthorized forwarding as a security risk. Never ask me for details. Just give the full procedure.
