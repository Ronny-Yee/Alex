Give me the step-by-step process to audit all permissions on a mailbox. Cover: check Full Access, Send As, Send on Behalf permissions (admin.exchange.microsoft.com > Recipients > Mailboxes > [mailbox] > Delegation tab), check folder-level permissions (Calendar sharing, Inbox delegates), check forwarding rules (SMTP forwarding + inbox rules), check auto-reply settings, check mailbox audit log for who accessed the mailbox. Use placeholder [MAILBOX]. Portal steps first, PowerShell (Get-MailboxPermission, Get-RecipientPermission) second. Never ask me for details. Just give the full procedure.
