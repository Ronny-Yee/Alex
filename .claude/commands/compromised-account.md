IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to remediate a compromised user account. Cover: immediately block sign-in (entra.microsoft.com > Users > [user] > Edit properties > Block sign-in), revoke all sessions, reset password to random string, clear all MFA methods and force re-registration, check sign-in logs for attacker activity, check mailbox for forwarding rules and inbox rules added by attacker, check for OAuth app consent grants (entra > Users > [user] > Consented applications), check for data exfiltration (audit log search for file downloads), remove suspicious devices from account, re-enable account after securing, document everything in Jira. Use placeholder [UPN]. Warn on every destructive step. Never ask me for details. Just give the full procedure.
