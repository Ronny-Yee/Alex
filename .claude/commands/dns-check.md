IMPORTANT: Do NOT ask for employee details, names, emails, departments, or any identifying info. Deliver the FULL procedure IMMEDIATELY using only placeholders like [FIRST_NAME], [UPN], [DEVICE_NAME], [DEPARTMENT]. The user will substitute real values themselves.

Give me the step-by-step process to troubleshoot DNS issues. Cover: verify DNS resolution (nslookup [DOMAIN] from client), check which DNS server the client is using, check Meraki DHCP DNS settings, check if internal DNS (AD DNS) is resolving correctly, test external DNS resolution, check DNS propagation for domain changes (MXToolbox or whatsmydns.net), flush DNS cache (ipconfig /flushdns), check for DNS poisoning or hijacking. Use placeholders. Command-line steps + Meraki dashboard. Never ask me for details. Just give the full procedure.
